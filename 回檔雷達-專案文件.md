# 回檔雷達 — 專案文件

> 台股回檔追蹤 PWA，快速查看自選股距 3 年高點的回檔幅度與下一個關鍵關卡價位。

---

## 一、專案概述

| 項目 | 內容 |
|------|------|
| App 名稱 | 回檔雷達 |
| 類型 | PWA（可安裝至手機主畫面） |
| 目標平台 | 手機優先（Mobile First） |
| 資料範圍 | 台股（上市 / 上櫃） |
| 設計風格 | 深色模式、高對比、等寬數字、看盤風格 |

---

## 二、核心功能規格

### 自選清單主畫面

每一列股票顯示兩行資訊：

**第一行**（代號 + 名稱）
```
2330  台積電  ↗
```

**第二行**（三欄等寬）
```
3年高          現價           下一關卡
2,440.00       2,385.00       918.00
               −1.65%        −15%
```

**顏色規則（回檔嚴重度）**
| 回檔幅度 | 顏色 |
|---------|------|
| 0 ～ 5% | 灰白 |
| 5 ～ 15% | 橘色 |
| 15% 以上 | 紅色 |

### 下一關卡計算邏輯

以目前回檔幅度無條件進位到最近的 5% 整數倍為下一關卡：
- 目前回檔 −12% → 下一關卡 **−15%**，顯示對應價格
- 目前回檔 −5%  → 下一關卡 **−10%**

### 展開明細（點擊列）

| 欄位 | 說明 |
|------|------|
| 距 3 年高點 | 回檔 % + 回檔金額 |
| 高點日期 | 3 年高點發生日期 |
| 今日漲跌 | 當日漲跌 %（紅漲綠跌） |
| 距下一關卡 | 距下一關卡的金額 |
| 關卡階梯 | 已突破（紅底）/ 下一個（藍框）/ 未來關卡 |
| 分享 | 明細底部獨立整行、靠右的低調分享鈕（見下） |

### 分享卡片成圖片

- 展開明細後，底部出現低調的灰色 icon +「分享」（**僅展開時顯示**），把整列卡片渲染成 PNG 分享出去
- 分享內容為**純圖片不帶文字**：含代號／名稱／現價／回檔／下一關卡 + 明細統計 + 關卡階梯
- 圖片裡那條 bar 移除分享鈕、改顯示來源列「回檔雷達 (YY-MM-DD HH:mm)」（截圖當下時間，靠右單行）
- 分享走 `navigator.share({ files })` 叫出系統分享單；桌面 / 不支援時退回下載 PNG

### 狀態列

```
● 盤中  最後更新 10:23:41
```
- 盤中（09:00–13:30）：綠點 + 「盤中」
- 盤後 / 週末：灰點 + 「盤後」

#### 標題列收合（節省空間）

- 清單**往下捲動**時，「回檔雷達」標題列（含 `+` 按鈕）平滑收合隱藏，把空間讓給清單
- **狀態列恆顯示**（盤中/盤後 + 最後更新時間始終可見，確保更新狀態一致）
- 標題列收合時，狀態列右側出現 `▾` 提示；**點擊狀態列即展開**標題列
- 捲回頂端（scrollTop ≤ 4）也會自動展開標題列

### 搜尋 / 新增股票

- 右上角 `+` 圓形按鈕
- 支援**代號前綴搜尋**（如 `006` → 列出所有 006x ETF）和**名稱搜尋**（如 `台積`）
- 搜尋來源為**台股官方完整清單**（上市 + 上櫃，約 2,370 檔），含全部股票與 ETF 的中文名
- 搜尋為**純本地比對**（零延遲）：代號前綴優先（依代號排序），其次名稱包含
- 搜尋框空白時顯示**前日熱門前 10**（依官方成交金額排序）
- 官方清單沒有的新上市代號：顯示「直接新增此代號」，即時從 Yahoo Finance 取得名稱（後備機制）
- 新增的股票加到清單**最後**
- **無任何寫死的股票清單**；首次使用者看到引導畫面，自選清單 100% 來自使用者操作

### 手勢操作

| 手勢 | 動作 |
|------|------|
| 點擊列 | 展開 / 收合明細 |
| 長按拖曳 | 手動排序 |
| 左滑 | 出現刪除按鈕 |
| 下拉 | 刷新資料 |

---

## 三、技術決策

### 技術棧（現行）

| 項目 | 決定 |
|------|------|
| 前端框架 | **Vue 3（`<script setup>` SFC）** |
| 打包工具 | **Vite 5** |
| PWA | **vite-plugin-pwa**（service worker、可離線、可安裝） |
| 樣式 | 純 CSS（深色主題，`src/styles/main.css`） |
| 字型 | **系統字堆疊**（無 web font）：數字 `ui-monospace`（iOS=SF Mono）、中文 `system-ui`/`PingFang TC`/`Noto Sans TC`。不依賴 Google Fonts |
| 卡片轉圖片 | **html-to-image**（分享功能，離屏 clone 渲染 PNG） |
| 資料儲存 | localStorage |
| 部署 | Vercel（免費方案，不需信用卡） |

> 早期原型曾以 React 18 + Babel CDN 單檔實作（來自設計稿），2026/06/05 已遷移到原定的 Vue 3 + Vite，並補上真正的 PWA 離線/安裝能力；舊原型 `回檔雷達.html` 已移除。

### API 架構

**問題：** 瀏覽器直接呼叫 Yahoo Finance / 證交所 API 會被 CORS 擋住。

**解法：** 同一份 proxy 路由邏輯（`proxy/handlers.js`）供開發與生產共用：
```
開發：瀏覽器 → /api/... → Vite 中介層（configureServer）→ 外部來源
生產：瀏覽器 → /api/... → Vercel Function（api/[...path].js）→ 外部來源
```

| 環境 | Proxy 方式 |
|------|-----------|
| 本機開發 | `npm run dev`（Vite，port 5175）內建 `/api` 中介層 |
| 生產（Vercel） | `api/[...path].js` catch-all serverless function |

**資料來源與端點：**

| 端點 | 來源 | 用途 |
|------|------|------|
| `/api/chart?symbol={代號}.TW&range=3y&interval=1d` | Yahoo Finance | 3 年日線（取高點 + 後備現價） |
| `/api/quote?ids=tse_2330.tw\|otc_6488.tw` | 證交所 MIS 即時行情 | **盤中即時現價**（批次、含昨收） |
| `/api/twse-list` | 證交所 OpenAPI `STOCK_DAY_ALL` | 上市完整清單（含成交金額） |
| `/api/tpex-list` | 櫃買 OpenAPI `tpex_mainboard_quotes` | 上櫃完整清單（含成交金額） |

> **⚠️ Vercel 路由踩坑（重要）：** 所有 `/api/*` 端點都必須是**單段路徑 + 參數放查詢字串**。
> Vercel 的 catch-all serverless function（`api/[...path].js`）對**多段路徑**（如 `/api/chart/0050.TW`）不生效會回 404，
> 含點號（`.TW`）的路徑還會被當成靜態檔。因此 `/api/chart` 的代號改放 `?symbol=` 查詢參數，
> 而非原本的 `/api/chart/{代號}`。本機(Vite)兩種都能跑、只有 Vercel 會出問題，務必以單段 + query 為準。

> **⚠️ Yahoo 上市/上櫃後綴：** Yahoo Finance 代號後綴依市場別不同 —— 上市(tse)用 `.TW`、
> 上櫃(otc)用 `.TWO`（例：8299 群聯為 `8299.TWO`，用 `8299.TW` 會查無 → 3 年高抓不到）。
> 抓 Yahoo 時依每檔 `market` 決定後綴（`fetchFull`/`fetchPrice` 收 market 參數）；
> MIS 則用 `tse_`/`otc_` 前綴。market 來自官方清單。直接新增未知代號時先試 `.TW` 再試 `.TWO`。

> **⚠️ 上游 timeout 競態（會崩 dev server）：** `pipeGet` 對同一回應只能 `writeHead` 一次。
> 舊版 timeout callback 先 `req.destroy()` 再 `writeHead(504)`，但 `destroy()` 會引發 `'error'` 事件，
> 使另一條路徑又 `writeHead(502)` → 丟 `ERR_HTTP_HEADERS_SENT`；此例外發生在非同步事件中無法被 catch，
> 直接讓整個 Vite dev server 行程崩潰退出。修正：用 `settled` 旗標確保回應只決定一次；
> 送錯誤前一律檢查 `res.headersSent`/`writableEnded`，已送出就只 `res.end()` 收尾、不再碰 header；
> 補 `up.on('error')`（串流中上游中斷只收尾）與遲到回應 `up.resume()` 排空避免 socket 洩漏。
> dev/prod 入口另對 `route()` 包 try/catch 擋同步例外（如畸形 `req.url` 讓 `new URL` 丟錯）。

> **⚠️ 分享卡片轉圖片踩坑（html-to-image）：** ① **字型**：app 改用系統字堆疊後（無 web font），系統字本就存在於 SVG 算繪環境、不需嵌入；用 `skipFonts: true` 跳過字型嵌入，圖片與 app 用同一套系統字、天生一致。（歷史：早期 app 用 Google Fonts 時，html-to-image 讀不到「跨來源樣式表」的 `cssRules` 會丟 SecurityError；後來連同此問題一併用「改系統字」根除——詳見下方字型決策。）② **clone 重播動畫**：離屏 clone 插入 DOM 會重播 `.row-detail` 的 `detailin` 淡入（opacity:0 起手），會截到空白展開區 → 截圖前對 clone 內所有元素設 `animation: none`。③ **窄螢幕換行**：SE2 等窄螢幕來源列時間 `(YY-MM-DD HH:mm)` 會在空白處斷行 → `white-space: nowrap` + `flex-shrink: 0`。④ 防誤觸：WatchList `onDown` guard 加入 `.share-btn`（沿用 `.ext-link` 模式），點分享不被手勢攔截、不收合卡片。

> **⚠️ 字型為何用系統字而非 Google Fonts（2026/06/09 決策）：** 原本中英都拉 Google Fonts（IBM Plex Mono + Noto Sans TC）。問題根因：**網頁的 web font 是跨來源、需連網送達**，衍生 ① FOUT、② Google CDN 依賴、③ **分享圖片字型不一致**（html-to-image 因跨來源 `cssRules` 無法讀取而嵌不進字型）。評估後以「架構最簡、不過度設計」為準，選擇**往減法走**：刪除 Google Fonts，全面改用系統字堆疊（CSS 本就有系統字 fallback，等於刪掉 index.html 的 3 行 `<link>`）。效果：app 與分享圖片**用同一套本機系統字、天生一致**，且甩掉外部依賴、無 FOUT、PWA 更純。代價：數字字形由 IBM Plex Mono 換成系統 SF Mono（iOS），等寬看盤風格保留。對 iOS-first 台股 app 也符合中文圈「中文用系統字」的常見實務（CJK 字型過大、不宜整套 web font 嵌入）。

> **股票清單為什麼用官方 API：** Yahoo 搜尋端點（`/v1/finance/search`）只回最相關約 7 筆、無法前綴搜尋；`/v1/finance/lookup` 能前綴但只有英文截斷名。兩者皆無法同時滿足「前綴搜尋 + 中文名」。台股官方 OpenAPI 提供全部上市櫃股票/ETF 的中文名與成交金額，免金鑰、每日更新，故改用官方清單 + 本地搜尋。

**現價為什麼改用證交所 MIS：** Yahoo 免費 chart 端點的報價會延遲約一個交易日（最新只到前一日收盤），盤中看到的「現價」其實是昨收。證交所 MIS（基本市況報導 `mis.twse.com.tw`）提供真正的盤中即時報價，故現價改抓 MIS、歷史高點仍用 Yahoo。

**現價來源優先序（盤中精準）：**
```
現價 = 證交所 MIS 即時報價           ← 交易時間貼合台股盤面
     ↓ MIS 失敗 / 該檔無 market 資訊
     Yahoo regularMarketPrice         ← 後備（約昨收等級）
3 年高點 = Yahoo 3y 日線（每週快取）
```
- MIS 取價優先序：成交價 `z` → 上次成交 `pz` → 買賣價中點 → 開盤 `o` → 昨收 `y`
  （集合競價瞬間 `z` 會是 `-`，故需後備）
- 整個自選清單以**一次批次請求**取得（`ex_ch` 以 `|` 串接，前綴 `tse_`/`otc_`）
- MIS 需 `Referer` header 防盜連，由 proxy server-side 設定

**股票清單與熱門推薦：**
- 啟動時抓取證交所 + 櫃買兩份官方清單，合併去重成約 2,370 檔 `{code, name, market}` 索引
- `market`（tse/otc）供 MIS 即時報價帶前綴用
- 同一份資料含成交金額（TWSE `TradeValue` / TPEx `TransactionAmount`），排序取前 10 為「前日熱門」
- 兩者存 localStorage（`radar_stocks`、`radar_hot`），**每日過期**自動更新

### 3 年高點計算方式

使用**盤中最高價 × 還原係數**，正確處理股票分割：

```
adj_high[i] = high[i] × (adjclose[i] / close[i])
3年高點 = max(adj_high)
```

> 說明：adjclose 為 Yahoo Finance 提供的還原收盤價，除以 close 得到當日的還原係數，再乘以當日最高價，即可得到分割調整後的真實最高點。

3 年高點每週快取、盤中凍結。**個股盤中突破已知 3 年高時，以證交所 MIS 當日最高（`s.h`）即時上修 `high3y`、高點日期改記今日**（避免回檔算成正值、下一關卡錯亂）；用「當日累積最高」而非現價，故盤中創高後又跌落仍正確。`high3y` 待下次 Yahoo 重抓以正式還原值校正。

### localStorage 快取策略

| 資料 | 鍵值 | 更新時機 |
|------|------|---------|
| 自選清單順序 | `radar_list` | 每次異動時 |
| 各股完整資料 | `radar_data` | 每次刷新時 |
| 3 年高點 | 包含在 `radar_data`（key: `_full3yAt`） | 超過 7 天重新抓取 |
| 台股完整清單 | `radar_stocks` | 超過 1 天重新抓取 |
| 前日熱門前 10 | `radar_hot` | 隨清單一併更新（每日） |

---

## 四、資料刷新機制

| 觸發 | 動作 |
|------|------|
| App 啟動 | 載入官方清單 → 抓所有自選股（3 年高點 + MIS 即時現價） |
| 手動下拉刷新 | MIS 批次抓即時現價；3 年高點超過 7 天一併更新 |
| 自動刷新 | **僅在交易時間（09:00–13:30）每 5 分鐘**執行一次 |
| 新增股票 | 立即抓取該股 3 年高點 + 即時現價 |

> 每次刷新流程：① 需更新 3 年高點者抓 Yahoo → ② 全部以 MIS 批次覆蓋即時現價 → ③ MIS 未涵蓋者用 Yahoo 後備。

---

## 五、本機開發方式

### 啟動開發環境

```bash
cd /Users/bevis/Documents/Claude/StockInformation
npm install      # 首次
npm run dev      # 開發（Vite，含 /api 中介層）
npm run build    # 產生 dist/（含 PWA service worker）
npm run preview  # 預覽 build 產物
```

開啟瀏覽器：`http://localhost:5175`

> Claude Code 預覽：`.claude/launch.json` 已設定為 `npm run dev`（Vite, port 5175），
> dev server 內建 `/api` proxy 中介層，不需另外手動啟動。

### 檔案結構

```
StockInformation/
├── index.html              # Vite 進入點
├── package.json / vite.config.js
├── public/icons/           # PWA 圖示（由 radar.svg 生成）
├── src/
│   ├── main.js  App.vue
│   ├── components/         # Icon, StatusBar, EmptyState, StockRow, SearchSheet, WatchList
│   ├── composables/        # useStockData（清單+熱門）、useWatchlist（自選+刷新）、useShareCard（卡片轉圖片+分享）
│   ├── api/index.js        # 股價/清單/MIS/快取/搜尋
│   ├── utils/format.js     # 格式化與計算
│   └── styles/main.css
├── proxy/handlers.js       # 共用 proxy 路由（dev 中介層 + Vercel 共用）
├── api/[...path].js        # Vercel serverless（catch-all）
└── 回檔雷達-專案文件.md      # 本文件
```

---

## 六、已確認功能 ✅

- [x] 自選清單主畫面（三欄佈局）
- [x] 股票代號 + 名稱獨立一行，完整顯示
- [x] 3年高點（還原股價，含分割調整）
- [x] 盤中突破 3 年高時以 MIS 當日最高即時上修高點
- [x] 現價 + 回檔 %（顏色標記嚴重度）
- [x] 下一關卡價格 + 趴數
- [x] 展開明細（高點日期、今日漲跌、關卡階梯）
- [x] 點擊展開 / 收合
- [x] 長按拖曳排序
- [x] 左滑刪除
- [x] 下拉刷新
- [x] 全台股搜尋（官方完整清單，代號前綴 + 名稱）
- [x] 前日熱門前 10 推薦（官方成交金額排序）
- [x] 盤中即時現價（證交所 MIS，批次、Yahoo 後備）
- [x] 直接輸入代號新增未知股票（後備：即時查詢名稱）
- [x] 名稱旁外開 Yahoo 股市連結（↗）
- [x] 分享卡片成圖片（展開明細 → PNG，Web Share / 下載後備，手機實測通過）
- [x] 盤中 / 盤後狀態顯示
- [x] 交易時間自動刷新（每 5 分鐘）
- [x] localStorage 快取（含清單每日更新）
- [x] 空清單引導畫面（雷達動畫，無預設股票）
- [x] **Vue 3 + Vite 架構**（取代 React CDN 原型）
- [x] **PWA service worker（可離線、可安裝）+ 圖示**

---

## 七、待完成項目 ⏳

### 高優先

> 來源：2026/06/10 全專案程式碼審查（詳見 **§九**）。

| 優先 | 項目 | 檔案 |
|------|------|------|
| 🟡 中 | 切回前景未立即刷新：自動刷新 timer 不理會分頁可見性，使用者切回看到舊價（非 bug，屬體驗缺口） | `App.vue` |

> **部署狀態（已上線、手機驗證通過）：** GitHub repo `lighthola/huidang-radar`（public）+ Vercel 自動部署。
> 正式網址 **`huidang-radar.vercel.app`**；任何分支 push 自動產生 preview 網址。
> 四個實測修正皆已合併進 main：Vercel 路由(#1)、手機下拉更新(#2)、刪除後重加(#3)、上櫃 3 年高(#4)。
> **手機已驗證：** 下拉更新、加入主畫面安裝、離線開啟。

### 低優先 / 未來考慮

| 項目 | 說明 |
|------|------|
| 🟢 外開連結上櫃後綴 | 名稱旁 ↗ 外開 Yahoo 連結對上櫃股仍用 `.TW`，應依市場別改 `.TWO`（`StockRow.vue`） |
| 🟢 `load()` 容錯 | `useStockData.load()` 無 try/finally；惟 `loadStockData` 永不拋（內含 try/catch 且一律回傳值），目前不會卡 loading，屬防禦性整理（`useStockData.js`） |
| 🟢 `sharing` 模組級單例 | `useShareCard` 的 `sharing` 為模組單例、所有列共用；目前僅單列展開無實際影響，多列展開才會出錯，可改為 `useShareCard()` 內 `ref`（`useShareCard.js`） |
| 🟢 拖曳等高假設 | `ROWH()` 取首列高度當所有列高度；各列由 CSS 固定等高、拖曳時皆收合，影響可忽略（`WatchList.vue`） |
| 🟢 full 判斷重複 | `addStock` 與 `fetchCodes` 各自判斷「是否已有完整 3 年資料」，邏輯重複；最壞僅在重加舊版快取股票時多抓一次 Yahoo，建議抽共用 helper（`useWatchlist.js`） |
| 🟢 `fetchQuotes` key 防呆 | MIS 回傳以 `s.c` 為 key 寫入 `data`，建議補防呆：`s.c` 不在送出 codes 內就略過（`api/index.js`） |
| 🟢 拖曳排序高頻寫入 | 拖曳中每跨一格就 `saveList` 寫一次 localStorage（payload 小，邊際），可延到 `onUp` 或 debounce（`WatchList.vue`） |
| 🟢 搜尋無 debounce | 每次按鍵對 ~2,370 檔線性掃描兩次，可加輸入 debounce（`api/index.js`/`SearchSheet.vue`） |
| 🟢 proxy CORS 全開 | `/api/*` 設 `Access-Control-Allow-Origin: *`，無資料外洩風險，惟他站可盜用為免費 proxy，若在意額度可限定 Origin（`proxy/handlers.js`） |
| 🟢 PWA 殘留 `woff2` | 已移除 web font，`vite.config.js` 的 `globPatterns` 仍列 `woff2`（無害殘留，可移除） |
| 深色 / 淺色主題切換 | 目前固定深色 |
| 回檔提醒 | 跌破某關卡時推播通知 |
| 分組標籤 | 將自選股分組管理 |

---

## 八、已知限制

| 項目 | 說明 |
|------|------|
| Yahoo Finance API | 非官方 API（僅用於 3 年高點 + 現價後備），無 SLA，報價延遲約一個交易日 |
| 證交所 MIS | 非官方文件、無 SLA，會限制高頻輪詢（本 app 每 5 分鐘批次一次，溫和） |
| MIS 盤後行為 | 盤後/假日可能無即時值，會退回買賣價中點或昨收；必要時 Yahoo 後備 |
| 官方清單時效 | 證交所/櫃買清單為「最近交易日」資料，盤中顯示前一交易日的熱門排序 |
| 3 年資料量 | 每股約 750 筆日線，10 支股票約 250KB |

---

## 九、程式碼審查結論（2026/06/10）

全專案（`.vue` / `.js` / proxy / 設定）完整審查：整體品質良好 —— 無記憶體洩漏（監聽器於 `onUnmounted` 清理、timer 有清）、無 XSS（無 `v-html`/`innerHTML`）、無 API 金鑰外洩、PWA 設定正確、抽象層級恰當無過度設計。發現的唯一 🔴（盤中創新高計算錯亂）已修正，行為見 §三；其餘待處理項見 §七。

---

*最後更新：2026/06/10（程式碼審查 §九；修正盤中創新高時 3 年高即時上修，見 §三）*
*歷次：2026/06/09 分享卡片成圖片、移除 Google Fonts 改系統字｜2026/06/05 部署 Vercel + 四項實測修正（路由、下拉更新、刪除後重加、上櫃 .TWO 後綴）｜更早 遷移 Vue 3 + Vite + PWA、MIS 即時報價、官方清單搜尋*
